apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-vol
  namespace: web
spec:
  accessModes:
    - ReadWriteOnce 
  storageClassName: "microk8s-hostpath"
  resources:
    requests:
      storage: 2Gi